# TenkoCall 中心運用 + HCE 分割セッション計画

Refs: [ippoan/sesame#1](https://github.com/ippoan/sesame/issues/1)（結論コメント 2026-07-12）

関連: `plan/nfc-universal-unit-st25r3916-note.md`（HCE + Custom Protocol の下回り検証）、
`plan/nfc-android-hce-verification.md`（既存 PCSC リーダーでの HCE 読取検証）、
`plan/cores3-hub-consolidation.md`（CoreS3 統合ハブ。本ドキュメントにより一部フローが差し替え）

## 方針（sesame#1 結論）

当面の目標は **alc-app + alc-app-s3 + TenkoCall の3構成での運用**とする。

| コンポーネント | 役割 |
|---|---|
| **alc-app** | Web フロントエンド / バックエンド連携（既存） |
| **alc-app-s3** | CoreS3 ハブ（FC-1200 RS232C・BLE 機器の集約） |
| **TenkoCall** | キオスク以外のすべて: **顔認証・点呼機能・車検証読み取り・免許証読み取り** |
| **AlcoholChecker** | **予備（フォールバック）**。運用上はキオスク（kiosk）役に限定 |

SESAME Face 静脈認証（キオスク側フォールバック生体認証）は予備扱い・保留
（sesame#1 参照。2026年8月末予定の CANDY HOUSE 公式 ESP32 SDK 更新後に再評価）。

## 点呼フロー（HCE 分割セッション）

```
TenkoCall (スマホ)                     キオスク (AlcoholChecker / CoreS3)
─────────────────                     ─────────────────────────────
① 顔認証 + 点呼開始
② 車検証・免許証読み取り (必要時)
③ HCE タッチ① ──────────────────────▶ 測定引き継ぎ
   (点呼セッショントークンを渡す)        │
                                       ▼
                                      FC-1200 アルコール測定
                                       │ 測定結果は rust-alc-api へ
                                       │ トークン紐付きで保存
④ HCE タッチ② ◀─────────────────────┘
   (測定完了確認・結果参照)
⑤ 点呼継続・完結 (TenkoCall 内)
```

- キオスクへ引き継ぐのは**アルコール測定だけ**。本人確認（顔認証）・点呼記録の主体は TenkoCall
- タッチ①とタッチ②は**別セッション**（測定中はスマホをリーダーから離す前提）。
  同一点呼への復帰は点呼セッショントークンで行う

### 顔認証の保証（BLE 端末距離 + S3 署名）

TenkoCall（個人スマホ）の顔認証単独では「どこで・どの端末で認証したか」の真正性を
担保できない。以下の方式で**キオスク現場での顔認証**であることを保証する:

1. TenkoCall で顔認証を実施
2. **BLE で alc-app-s3 (CoreS3) との端末距離を確認**（現場近接の証明）
3. S3 と通信し、**顔認証結果に S3 側で署名を付与** → 「現場で行われた顔認証」として保証

これにより QR 方式（スクリーンショット複製リスクで不採用）を使わずに、
識別（顔認証）と現場性（BLE 近接 + S3 署名）を結合できる。

### 設計課題（要検討）

- [ ] **点呼セッショントークン設計**: ワンタイムトークン + 署名 + 短期失効。
      `ippoan/rust-alc-api#480` / `ippoan/alc-app#72` の device pairing (RFC 8628 スタイル)
      実装パターンの流用可否を確認（リプレイ攻撃対策を含む）**（未検証のまま残存）**
- [ ] **BLE 端末距離確認の閾値設計**: RSSI ベースの近接判定（距離精度・すれ違い誤判定の排除）、
      CoreS3 側アドバタイズ/GATT 設計
- [ ] **S3 署名の鍵管理**: CoreS3 の署名鍵のプロビジョニング（device credential /
      auth-worker device pairing の既存パターン流用）と rust-alc-api 側の検証
- [ ] **測定結果の返却経路の選定**（3案）:
  - **案A: サーバー経由**（タッチ②は「完了通知」のみ、結果は rust-alc-api から取得）
    → キオスク→スマホ方向の CE ハード不要。**推奨・まずこれで設計**
  - 案B: タッチ②で HCE 応答に結果を同梱（キオスク側リーダーがチャレンジ提示）
    → 既存 PCSC リーダーで可（スマホ側 HCE が応答するだけ）
  - 案C: キオスク側 CE（M5 NFC Unit / ST25R3916）でスマホが読み取る
    → 新規ハード必要。`nfc-universal-unit-st25r3916-note.md` の「キオスク発信」フロー
- [ ] タッチ①→測定→タッチ②の**タイムアウト設計**（測定放棄・別人割り込みの排除）
- [ ] キオスク側の同時利用（複数ドライバーの点呼セッションが並ぶ場合）の排他制御

## TenkoCall 側の実装項目

> **顔認証・免許証読み取り・車検証読み取りは AlcoholChecker で実装済み**。
> TenkoCall 側はゼロからの技術検証ではなく、**AlcoholChecker からの移植**が中心となる。

### 顔認証（AlcoholChecker 実装済み → 移植）

- [ ] AlcoholChecker の顔認証実装（face-api）を TenkoCall へ移植
      （コード流用範囲の確認。alc-app web/ の human.js 実装も参考）
- [ ] 顔テンプレートの保存先（端末ローカル vs rust-alc-api）と、
      **個人スマホでのなりすまし対策**（他人端末での認証・登録顔差し替えの防止）
      → 現場性の担保は「顔認証の保証（BLE 端末距離 + S3 署名）」節の方式で行う

### 点呼機能

- [ ] 点呼記録の項目定義（改善基準告示・点呼規程との整合）
- [ ] rust-alc-api 側の点呼セッション API / スキーマ設計（measurements との紐付け）

### 免許証読み取り（AlcoholChecker 実装済み → 移植）

- [ ] AlcoholChecker の免許証読み取り実装を TenkoCall へ移植
      （ISO14443 Type-B + 暗証番号の読取ロジック・PIN 入力 UX を流用）
- [ ] rust-alc-api 側スキーマとの接続確認（employees への免許情報紐付け・有効期限アラート）

### 車検証読み取り（AlcoholChecker 実装済み → 移植）

- [ ] AlcoholChecker の電子車検証読み取り実装を TenkoCall へ移植
- [ ] rust-alc-api 側スキーマとの接続確認（車両マスタ・車検有効期限アラート）

## 規制面の再確認（キオスク顔認証 → スマホ顔認証への変更に伴う）

`cores3-hub-consolidation.md` の規制検討は**キオスク側顔認証**を前提に書かれている。
本方針では本人確認が個人スマホ側に移るため、以下を再確認する:

- [ ] 遠隔点呼（告示第5条）「生体認証符号等により確実に識別する機能」を
      **個人スマホの顔認証 + BLE 端末距離 + S3 署名**（「顔認証の保証」節）で
      満たせるかの整理
- [ ] 「全身+検知器使用状況の随時確認カメラ」要件は引き続き既存の監視カメラ運用
      （ippoan/alc-app#27 の ONVIF 死活監視）で満たす前提を維持
- [ ] 識別（TenkoCall 顔認証）と測定（キオスク FC-1200）が**別デバイス・時間差**になるため、
      なりすまし・混入の余地がないこと（BLE 近接 + S3 署名 + HCE トークンによる
      場所・時間の結合で担保）の整理

## 既存 plan との関係・差し替え

- `cores3-hub-consolidation.md` の「顔認証（タブレット側カメラ）→ QR → 測定」フローは
  **本方針で差し替え**。QR は `nfc-universal-unit-st25r3916-note.md` で
  スクリーンショット複製リスクにより不採用済み。識別と現場性の結合は
  **顔認証（TenkoCall）+ BLE 端末距離確認 + S3 署名**（「顔認証の保証」節）で行い、
  測定引き継ぎは HCE で行う
- `nfc-android-hce-verification.md` の「既存 PCSC リーダーで SELECT AID→レスポンス往復」検証は、
  本方針の**クリティカルパス**に格上げ（タッチ①の成立確認そのもの）

## AlcoholChecker の役割縮小（本ドキュメントで正式記載）

AlcoholChecker（Device Owner Kiosk 端末）は本方針により以下の位置づけとする:

- 通常運用では **kiosk 役に専任**: FC-1200 測定、HCE タッチ受け（PCSC リーダー）、
  alc-app-s3 (CoreS3) / rust-alc-api との連携のみを使用
- 実装済み機能（顔認証 face-api・免許証読み取り・車検証読み取り・NFC 社員カード読取）は
  **削除せず温存**する。TenkoCall 系統の障害時に**予備（フォールバック）として
  単独フル機能運用へ切り替え可能**とするため
- 温存機能は TenkoCall への**移植元**としても使用する（「TenkoCall 側の実装項目」参照）

## 検証チェックリスト（優先順）

1. [ ] 既存 PCSC リーダー + 最小 HCE テストアプリで SELECT AID→レスポンス往復の疎通確認
       （`nfc-android-hce-verification.md` の手順）
2. [ ] BLE 端末距離確認 + S3 署名による顔認証保証の PoC
       （CoreS3 アドバタイズ→TenkoCall 近接判定→署名付与→rust-alc-api 検証）
3. [ ] 点呼セッショントークンの引き継ぎ→復帰（タッチ①→②）を案Aで PoC
       （ワンタイムトークン + 署名 + 短期失効は未検証のまま。ここで消化する）
4. [ ] AlcoholChecker → TenkoCall への移植可否確認（顔認証・免許証・車検証の
       コード流用範囲の見極め）
5. [ ] 規制面の整理（上記チェック項目）を点呼規程ドラフトに反映
